# SOEMS Production Environment Template
# Copy this to .env and fill in the values

# --- Backend Configuration ---
PORT=5001
MONGO_URI=mongodb://mongo:27017/soems
JWT_SECRET=your_super_secret_production_key_here
NODE_ENV=production

# --- Frontend Configuration ---
# In a standard Docker Compose deployment, the frontend accesses the backend via the proxy at /api.
# Set VITE_API_URL to the public domain if needed, otherwise leave blank to use relative paths.
VITE_API_URL=
FRONTEND_URL=http://localhost # The public-facing URL of your frontend
